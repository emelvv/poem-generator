<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{{title}}}</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="main_page.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="/">Poem Generator</a>
            <ul class="navbar-nav mr-auto">
                {{#if username}}
                <li class="nav-item">
                    <a class="nav-link" href="/auth">Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">Main Page</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/auth">Sign In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/auth">Sign Up</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">Main Page</a>
                </li>
                {{/if}}
            </ul>
            {{#if username}}
            <div class="status-container">
                <span id="status-text">{{{username}}}</span>
            </div>
            {{/if}}
        </nav>
    </header>
    {{{ body }}}
    <footer>
        <p>&copy; 2024 Poem Generator | All rights reserved</p>
    </footer>
</body>
</html>